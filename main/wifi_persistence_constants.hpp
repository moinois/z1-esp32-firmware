// Defines the shared NVS identifiers used by station credential adapters.
#pragma once

namespace firmware::target::wifi_persistence {

inline constexpr char name_space[] = "wifi_config";
inline constexpr char ssid_key[] = "ssid";
inline constexpr char password_key[] = "password";

}  // namespace firmware::target::wifi_persistence
