# Requirement traceability

This file records implemented and verified specification slices. A requirement
is marked complete only when its portable logic has a host test and any required
ESP-IDF behavior has either a target test or a documented hardware test.

| Area | Implemented | Host verified | Hardware verified |
|---|---|---|---|
| FRM-001--FRM-016 common framing and recovery | Core implementation | Yes for envelope, length validation, CRC/tail rejection, stream boundaries, recovery modes, and transport limits | No |
| USB-007 transmit queue | Application implementation | Yes for 30-frame FIFO, 544-byte bound, empty/overflow rejection, completion-driven removal, and producer/consumer mutual exclusion | TinyUSB transport integration and physical verification pending |
| USB-006 receive staging | Application implementation | Yes for 32768-byte capacity, complete-block admission, whole-session discard on overflow, and clear-on-disconnect | TinyUSB transport integration and physical verification pending |
| USB-004--USB-005, USB-009--USB-010 protocol activity state | Application implementation | Yes for enumeration gating, valid-frame activation, disconnect receive clearing, atomic send gating, retained transmit queue, and shared play-owner disconnect notification | TinyUSB mount/unmount/RX callbacks, controller forwarding, and USB ownership disconnect hook target-built; TX routing and physical verification pending |
| USB-001--USB-003 descriptors | Application implementation | Yes for device identity, vendor interface, bulk endpoint geometry, bus power, and required strings | TinyUSB dependency, vendor interface configuration, and startup adapter target-built; physical enumeration pending |
| USB-008 transmit progress timeout | Application implementation | Yes for strict 500 ms no-progress expiry, progress reset, and explicit clear | TinyUSB TX task integration target-built; physical verification pending |
| USB local recording control | Target adapter over recording command policy | Target-built for M951/M952 state updates and queued responses | Physical USB verification pending |
| USB local update/diagnostic/version commands | Target adapter over update trigger and controller snapshots | Target-built for upgrade/reset triggers and queued diagnose/version responses | Physical USB verification pending |
| USB local status snapshot | Target adapter over aggregated runtime status and controller snapshots | Target-built for USB `?` status routing, shared runtime extension, and queued snapshot response | Physical USB/runtime verification pending |
| USB streamed-play preparation | Target adapter over shared play session and ownership policy | Target-built for USB `play` preparation, POSIX file/MD5 lookup, USB status response, shared play ownership, and TCP broadcast forwarding | Physical USB/controller/filesystem verification pending |
| USB file upload | Target POSIX upload port over `FileUpload` policy | Target-built for USB upload admission, bounded POSIX writes, MD5 sidecar creation, retries/timeouts, ownership, and USB response queueing | Physical USB/storage verification pending |
| USB file download | Target POSIX download port over `FileDownload` policy | Target-built for USB download admission, MD5/cache lookup, bounded offset reads, response workspace checks, retries/timeouts, ownership, and USB response queueing | Physical USB/storage verification pending |
| USB M942 CAN exercise | Target adapter over shared CANopen SDO service and M942 policy | Target-built for USB M942 admission, globally shared single-worker capacity across TCP/USB, SDO access, controller forwarding, delayed responses, and USB-origin response routing | Physical USB/CAN verification pending |
| USB local serial-number control | Target adapter over NVS and serial-number service | Target-built for `sn-get`/`sn-set`, shared operation admission, persistence delegation, and queued responses | Physical USB/NVS verification pending |
| USB local runtime control | Target adapter over NVS and runtime command service | Target-built for `sys-time`/`clearftm`, UTC formatting, erase semantics, shared admission, and queued responses | Physical USB/NVS verification pending |
| USB local filesystem mutations | Target POSIX filesystem port over filesystem command policy | Target-built for `mkdir`, `rm`, `mv`, `ftype`, recursive removal, serialized local-command dispatch, and queued responses | Physical USB/filesystem verification pending |
| USB local directory and MD5 queries | Target POSIX/mbedTLS query ports over application services | Target-built for `ls`, `md5sum`, metadata enumeration, regular-file checks, and queued responses | Physical USB/filesystem verification pending |
| USB local configuration commands | Target POSIX configuration ports over configuration policies | Target-built for `config-get`, `config-set`, `config-restore`, and `config-default` routing, serialized local-command dispatch, and queued responses | Physical USB/storage verification pending |
| USB local WLAN scan | Target ESP-IDF scan port over WLAN scan policy | Target-built for USB `wlan scan` parsing, bounded blocking scan, observation conversion, and queued responses | Physical USB/RF verification pending |
| USB local WLAN connection control | Target ESP-IDF station port over manual connection policy | Target-built for USB `wlan connect`/`wlan disconnect`, bounded credentials, state polling, NVS persistence, discovery burst, serialized local-command dispatch, and queued responses | Physical USB/RF/NVS verification pending |
| ROUTE-013, ROUTE-017 USB host routing | Target adapter over shared host router | Target-built for local `0xb7` play-status responses and suppression of ordinary USB forwarding while controller transfers are active | Physical USB/controller verification pending |
| ESC-001--ESC-002 escaping | Core implementation | Yes | Not required |
| HFT-004 path normalization | Core implementation | Yes | Not required |
| HFT-001--HFT-003 transfer start and path validation | Core implementation | Yes | TCP service integration and target build verified; runtime filesystem verification pending |
| HFT-010--HFT-011 cache path mapping | Core implementation | Yes | Target adapter now provides parent-directory creation |
| HFT-013 cached MD5 extraction | Core implementation | Yes | Target adapter now provides POSIX/mbedTLS MD5 calculation |
| HFT-014 cache base preparation | Download port contract | Invocation verified | Target adapter and TCP service routing implemented |
| HFT-005--HFT-007 start admission and owner mailbox | Application implementation | Yes | TCP ownership and per-session service integration target-built; runtime contention verification pending |
| HFTD-001--HFTD-010 host download | Application implementation with replaceable port | Yes | POSIX file, bounded ESP-IDF response-workspace allocation, TCP queue, per-session routing, and ownership lifecycle implemented |
| HFT-020--HFT-021, HFT-023, HFT-025 download timing and errors | Application implementation | Yes for download | ESP timer integration target-built; runtime timing verification pending |
| HFTU-001--HFTU-010 host upload | Application implementation with replaceable port | Yes | POSIX file, TCP queue, per-session routing, and ownership lifecycle implemented |
| HFT-020--HFT-022, HFT-024--HFT-025 upload timing and retries | Application implementation | Yes for upload | ESP timer integration target-built; runtime timing verification pending |
| FILE-001--FILE-003 common filesystem path syntax | Core implementation | Yes | Not required |
| FILE-010 directory-list argument and option parsing | Core implementation | Yes | Not required |
| FILE-011--FILE-015 directory listing | Application implementation with replaceable port | Yes | POSIX enumeration and TCP response adapter implemented |
| FILE-020--FILE-026 filesystem mutations and type reply | Core parsing and application implementation with replaceable port | Yes | POSIX recursive removal and TCP response adapter implemented |
| FILE-027--FILE-029 MD5 command | Application implementation with replaceable metadata and hash port | Yes | POSIX/mbedTLS hashing and TCP response adapter implemented |
| SD-001--SD-008 SD-card lifecycle and capacity | Application implementation with replaceable port | Yes for policy and ordering | GPIO, SDMMC, FAT, and logging adapters pending |
| SD-009--SD-010 FAT filename, sector, and lock policy | Target defaults | Build verification for code page 437, short names, 4096-byte sectors, 16 lock slots, and 10-second timeout | Physical-card verification pending |
| CFG-002--CFG-003 SD configuration line parsing | Core implementation | Yes | Not required |
| CFG-010 configuration command tokenization | Core implementation | Yes | TCP and USB configuration-service integration target-built; physical storage verification pending |
| CFG-001, CFG-004--CFG-006 configuration file copies | Application implementation with replaceable bytewise file port | Yes | POSIX bytewise adapter and TCP command routing integrated; physical storage verification pending |
| CFG-020--CFG-023 live configuration view | Core chunk parser and application implementation with replaceable load port | Yes | POSIX chunk adapter and TCP command routing integrated; physical storage verification pending |
| CFG-011--CFG-013 configuration reads | Application implementation with replaceable live/SD read port | Yes | POSIX line adapter and TCP response routing integrated; physical storage verification pending |
| CFG-030--CFG-034 configuration updates | Core rewrite logic and application implementation with replaceable persistence port | Yes | POSIX temporary-file/rename adapter and TCP response routing integrated; physical storage verification pending |
| LOG-001--LOG-006 diagnostic capture and record formatting | Application implementation with replaceable output/buffer/clock port | Yes | ESP-IDF log-hook and SD-writer integration target-built; physical logging verification pending |
| LOG-007--LOG-013 diagnostic log writer | Application implementation with replaceable buffered-file port | Yes for session, flush, rotation, short write, and shutdown policy | SD filesystem/task integration target-built, including global-capture shutdown drain; physical logging verification pending |
| NET-001--NET-002 machine name | Core implementation | Yes | Synchronous SD boot-mount, config-file/MAC derivation, and connectivity integration target-built; physical card verification pending |
| NET-004--NET-005 AP channel selection | Core implementation | Yes for counting, wrapping, tie, and fallback | ESP-IDF startup scan/channel adapter target-built; physical RF verification pending |
| NET-003--NET-005, NET-008 connectivity startup | Application policy plus ESP-IDF `ConnectivityStartupAdapter` | Target build verified for station-only scan, channel selection, AP/STA configuration, hostname, power-save policy, fallback, and fatal failures | Physical RF and network-interface verification pending |
| NET-006--NET-007 socket capacity and hostname | Target defaults and application startup configuration | Exact values host verified; target build verified | STA hostname application and socket-capacity configuration target-built; runtime resource-exhaustion verification pending |
| NET-010--NET-017 saved credentials and automatic retry | Application policy plus ESP-IDF `AutomaticConnectionAdapter` | Target build verified for NVS credential loading, delayed association task, disconnect retry forwarding, and nonfatal storage outcomes | Physical station-event timing and RF verification pending |
| NET-020--NET-026 manual station lifecycle | Application implementation with ESP-IDF station/event adapters | Target build verified for connection phases, event data, RSSI/persistence policy, disconnect retention, and BLUFI event propagation | Physical station and BLE verification pending |
| NET-030--NET-033 Wi-Fi scan policy | Core result policy and application command implementation with replaceable Wi-Fi port | Yes | ESP-IDF TCP/BLUFI Wi-Fi scan adapters target-built; physical RF verification pending |
| NET-040--NET-043 host WLAN parsing and scan command | Core parser and application implementation with replaceable Wi-Fi port | Yes | TCP/USB command routing and ESP-IDF Wi-Fi adapters target-built; physical RF verification pending |
| NET-044--NET-046 host WLAN connect/disconnect responses | Application implementation with replaceable response and discovery port | Yes | TCP/USB command routing, discovery, and ESP-IDF Wi-Fi adapters target-built; physical RF verification pending |
| DISC-001--DISC-008 UDP discovery | Core formatting/address policy and application implementation with replaceable socket/clock port | Yes for payloads, ordering, timing, recreation, ignored failures, and runtime machine-name selection | ESP-IDF UDP socket, periodic task, station event, command-burst integration, and boot-derived machine-name wiring target-built; physical network verification pending |
| BLE-001--BLE-006 provisioning identity and lifecycle | Application policy plus ESP-IDF `BlufiLifecycleAdapter` | Target build verified for BLE-only initialization, `BLUFI_DEVICE` identity, public 160 ms all-channel advertising, preferred connection interval, reconnect advertising restoration, BLUFI profile startup, and nonfatal startup path | GATT/advertising-air-interface and physical BLE verification pending |
| BLE-010--BLE-016 provisioning commands | Application implementation plus ESP-IDF `BlufiProvisioningAdapter`/`BlufiCallbackAdapter` | Target build verified for station actions, scan conversion, status/list/error reports, advertising, custom-data logging, and supported event forwarding | Full encrypted GATT session and physical BLE verification pending |
| BLESEC-001--BLESEC-006 BLUFI security | Application policy plus ESP-IDF `BlufiCryptoAdapter` and callback bridge | Target build verified for bounded DH, MD5, AES-CFB128, negotiation response, encryption/decryption, and CRC callback wiring | Physical BLE verification pending |
| BWF-010--BWF-023, BWF-033 frame envelope, security, and acknowledgement | Application implementation with replaceable cipher and notification port | Yes for exact envelope, sequences, security flags, checksum order, encryption scope, failures, acknowledgement timing, and composed dispatch | ESP-IDF GATT adapter pending |
| BWF-030--BWF-032 outgoing fragmentation and incoming reassembly | Application implementation with replaceable frame/allocation/error port | Yes for MTU capacity, prefixes, exact completion, retained state, reset, and deterministic errors | Wire/GATT composition and physical-MTU verification pending |
| BWF-040--BWF-044 product subtypes and report payloads | Application dispatcher with replaceable provisioning/security/wire port plus payload encoders | Yes for recognized and ignored subtypes, exact version, error shape, status TLVs, Wi-Fi-list records, and composed input/output | Provisioning-action and ESP-IDF adapters pending |
| BWF-003, BWF-005--BWF-007, BWF-045 deterministic GATT policy | Application implementation with replaceable allocation/callback/notification port | Yes for fixed reads, response ordering, descriptor writes, aggregate limits, echoes, execute/cancel, reset, and retry | Attribute schema, unsafe overlap/gap behavior, and ESP-IDF GATT adapter pending |
| CMD-001--CMD-003 recognition and limits | Core implementation | Yes for ordered case-sensitive matching, argument offsets, and bounded/unbounded command rules | Not required |
| CMD-004--CMD-005 local command queue | Application implementation | Yes for 32-entry FIFO, 544-byte frame limit, immediate overflow rejection, serialized dequeue contract, and USB producer/consumer mutual exclusion | TCP, controller, and USB task integration target-built; runtime verification pending |
| ROUTE-001--ROUTE-018 routing policy | Application implementation | Yes | TCP, USB, and controller target dispatch integrations built; runtime transport verification pending |
| OWN-001--OWN-008 ownership policy | Application implementation | Claim, identity, independence, disconnect verified | TCP file/play claim, terminal release, disconnect cleanup, and target build verified; runtime contention verification pending |
| LPC-001--LPC-002 periodic controller queries | Application scheduler plus ESP-IDF controller UART task | Target build verified for periodic status/diagnostic query emission and bounded serial transport | Physical controller timing verification pending |
| LPC-003 initial controller status | Controller UART task using scheduler at startup | Target build verified for immediate first query opportunity | Physical controller verification pending |
| STAT-001--STAT-010 controller snapshots and replies | Core and application implementation | Yes | UART snapshot retention and TCP `?`/`diagnose`/`version` routing integrated; physical verification pending |
| UART-003--UART-008 controller link policy | Application implementation | Yes | Controller activity monitor, bounded UART adapter, and write diagnostics integrated; physical UART verification pending |
| DIAG-021 controller write failure message | Application implementation | Yes | Controller UART, runtime-command, and wall-clock response adapters emit the specified diagnostic; physical UART verification pending |
| HW-020--HW-022 controller UART configuration | ESP-IDF adapter | Exact settings verified | Adapter startup deferred by BOOT-012 |
| UART-002 bounded controller reads | ESP-IDF adapter | Exact settings verified | Adapter startup deferred by BOOT-012 |
| LPC-010--LPC-014 common controller-transfer wire layer | Application implementation | Yes for framing, queue, parsing, family dispatch, and host-forwarding suppression | Controller-task service integration and TCP router transfer gating target-built; physical controller verification pending |
| LPC-015--LPC-019 common transfer state behavior | Application implementation across firmware, configuration, and factory families | Yes for all three family state machines and their replaceable ports | Physical controller verification pending |
| LPCFW-001--LPCFW-006 controller firmware transfer | Application implementation with replaceable port | Yes | POSIX storage, UART response, controller-task routing, and machine-status phase/progress publication integrated; physical controller verification pending |
| LPCCFG-001--LPCCFG-006 controller configuration transfer | Application implementation with replaceable port | Yes | POSIX storage, UART response, and controller-task routing integrated; physical controller verification pending |
| LPCFAC-001--LPCFAC-005 controller factory-data transfer | Application implementation with replaceable port | Yes | POSIX storage, UART response, removal, and controller-task routing integrated; physical controller verification pending |
| PLAY-001--PLAY-007 streamed-play preparation and status | Application implementation with replaceable port | Yes for preparation and status | Shared TCP/controller session, POSIX storage, and routing integrated; physical verification pending |
| PLAY-010--PLAY-011, PLAY-018 play controller lifecycle | Application implementation with replaceable port | Start, validation, observer, terminal, UART routing, POSIX file adapter, shared TCP preparation, claim/release, and disconnect cleanup implemented | Physical controller verification pending |
| PLAY-007, PLAY-014 streamed-play logical lines | Application implementation with replaceable source | NUL, chunk, length, EOF, failure cases, data aggregation, and goto aggregation | Physical controller verification pending |
| PLAY-012--PLAY-017 streamed-play data exchange | Application implementation with replaceable source | Validation, aggregation, rewind, retry, EOF, cleanup, UART routing, POSIX source, and shared session implemented | Physical controller verification pending |
| PLAY-019--PLAY-020 streamed-play goto exchange | Application implementation with monotonic-time port | Validation, scan, pacing, target, EOF, failure cases, UART routing, and shared session implemented | Physical controller verification pending |
| PLAY-021 streamed-play family inbox | Shared application family inbox | Yes | Controller-task F-family routing integrated; physical verification pending |
| UPD-010--UPD-014 package validation | Core parser plus application orchestration and ESP-IDF `EspImageValidator` | Target build verified for header, segment-bound, and checksum validation delegation | Full OTA/storage composition and physical update verification pending |
| UPD-004--UPD-005, UPD-020--UPD-022 aggregate loading and validation effects | Application implementation with replaceable storage/phase/host/controller port | Yes for paths, ordering, absent reset, nondestructive failures, short reads, and format rejection | POSIX aggregate loading, attribute clearing, NVS phase clearing, routing, and ESP image adapters target-built; physical storage verification pending |
| UPD-060--UPD-063 update-file deletion | Application implementation with replaceable filesystem/delay/broadcast port | Yes for attempt bound, busy and permission recovery, exact delays, stopping rules, and final error | POSIX/FAT error mapping, attribute/mode recovery, FreeRTOS delays, update-task composition, and all-TCP host broadcast target-built; physical filesystem verification pending |
| UPD-023, UPD-040--UPD-043 update recovery and persistence | Application phase policy plus NVS byte adapter and update-task boot recovery | Target build verified for `ota_state/phase` persistence, boot readback, aggregate-opened phase clearing, and completed-phase-4 clearing | Failure broadcast scheduler and physical NVS verification pending |
| UPD-050--UPD-052, UPD-054 visible update status | Application implementation | Yes for persisted/volatile selection, rounded controller progress, clamping, and three-second success display | Machine-status/controller-progress composition and three-second target completion timer target-built; physical verification pending |
| UPD-030--UPD-034 validated package application | Application implementation plus ESP-IDF `OtaUpdateAdapter` | Target build verified for inactive-partition selection, bounded OTA write/finalize/boot selection, abort, controller staging, exact queued `0xa2 reset\\0` reset frame, aggregate removal, and restart calls | Physical OTA/UART verification pending |
| UPD-053--UPD-055 controller update handoff | Application monitor with replaceable staged-file/channel/controller/phase port | Yes for failure availability gates, qualifying timeout, completion ordering, immediate five-second schedule, and channel suppression | Target scheduler, controller-channel activity gates, reset routing, staged-file deletion, and phase composition target-built; physical controller verification pending |
| UPD-001--UPD-003 aggregate paths and triggers | Application trigger plus ESP-IDF `FirmwareUpdateAdapter` task composition | Target build verified for boot processing, exact file loading, validation/application ordering, and coalesced TCP/USB/controller-UART `upgrade`/`reset` requests | Physical storage verification pending |
| RUN-010--RUN-016 serial number | Application implementation with replaceable capacity/persistence/response port | Yes for exact namespace/key, syntax, trimming, length, characters, busy, read mappings, immutability, writes, and responses | Shared eight-slot executor, NVS, and origin-aware routing adapters target-built; physical verification pending |
| RUN-020--RUN-023 wall clock | Application implementation with replaceable clock/runtime/diagnostic/response port | Yes for exact query, positive int64 parsing, silent invalid/set paths, zero microseconds, first-boot request, UTC fallback, and diagnostics | System-clock, shared runtime capacity, logging, and origin-aware routing adapters target-built; physical verification pending |
| RUN-030--RUN-035 first boot and runtime counters | Application implementation with replaceable persistent-counter port | Yes for namespace/keys, missing defaults, first-boot immutability, per-save whole seconds, play intervals, redundant notifications, silent failures, and boot-time first-boot capture | Shared runtime capacity, NVS, monotonic clock, play, media-monitor, and runtime-task startup composition target-built; physical verification pending |
| RUN-040--RUN-043 runtime commands | Application implementation with replaceable capacity/persistence/UTC/response port | Yes for syntax, busy, persisted reads, namespace failure ordering, individual fallbacks, exact formatting, first_boot-only erase, and responses | Shared executor, NVS, UTC, and origin-aware TCP/USB/controller routing adapters target-built; physical persistence verification pending |
| RUN-050--RUN-052 aggregated runtime status | Application composition service with replaceable runtime-source port | Yes for transfer, recording, SD subtraction/fallback, update report, and RSSI fallback | TCP aggregation, controller snapshots, SD, live update phase, and station RSSI adapters integrated; physical verification pending |
| CAN-002--CAN-003, CAN-005--CAN-006 CANopen node identity and NMT | Portable core, application composition, TWAI error policy, and optional ESP-IDF service task | Yes for derived identifiers, immediate boot-up, drift-free 10 ms processing, retained heartbeat timing, addressed/broadcast NMT, delayed target reset, observed error bits, dictionary consistency, and physical bus-error mapping policy | Physical CAN verification pending |
| OD-001--OD-052 CANopen object dictionary | Portable core dictionary with typed access metadata, expedited SDO server, PDO mapping validation, receive-PDO router, event-timed TPDO scheduler, and write effects | Yes for all specified objects, defaults, access, little endian, retained writes, wire aborts, RPDO mapping validation/routing, event-timed TPDO mapping, and digital I/O | Segmented transfer (unnecessary for current scalars), heartbeat/error composition, and diagnostic-output adapter pending |
| CAN-002 SDO configuration | Portable expedited SDO server, named timing policy, composed effects, and target service loop | Yes for server/client timeout values, block disablement, exact request identifiers, scalar uploads/downloads, aborts, heartbeat updates, digital state, and target transmission | Physical CAN verification pending |
| CAN-010--CAN-015 M942 digital-I/O exercise | Application service with replaceable routing, clock, scheduling, and SDO-client port | Yes for unconditional forwarding, conditional replies, single exercise, exact addresses/patterns, zero latch, retries, delays, lock wait, and absolute deadline | Expedited SDO codec/mailbox, CANopen-task routing, TCP-local response, and dedicated FreeRTOS worker implemented; physical CAN and runtime concurrency verification remain pending |
| CAN-001, HW-050--HW-052 TWAI hardware | Portable configuration plus active optional ESP-IDF classic-CAN adapter | Exact mode, rate, timing, pins, filter, queues, unused outputs, nonfatal startup, and frame conversion host/target-build verified | Physical bus verification pending |
| DIAG-033 CAN digital-output monitoring | Application sampler plus ESP-IDF log/task adapter | Yes for exact startup records, 50 ms scheduling, initial zero, changed samples, collapse, hex format, and DO2 bit | Physical diagnostic-output verification pending |
| CAM-001--CAM-002, CAM-005, CAM-008 camera settings | Application one-shot loader and dimension mapping with replaceable first-value source | Yes for strict signed parsing, defaults, normalization, all mappings, fallback, load-once behavior, and CFG-002 ASCII-trimmed target parsing; target camera startup and recording task now consume normalized `/sd/config.txt` values | Physical sensor/SD verification pending |
| CAM-003, CAM-009, HW-040--HW-043 camera hardware | ESP-IDF esp32-camera adapter over the target-neutral hardware contract | Exact DVP/SCCB/XCLK pins, unused controls, JPEG, quality, PSRAM, buffer/grab policy, UXGA allocation, orientation, resolution mapping, dependency resolution, and target build verified | Physical sensor verification pending |
| WEB-001--WEB-002, WEB-004--WEB-005 server and web-volume configuration | Target-neutral immutable contracts plus ESP-IDF HTTP/SPIFFS adapters | Target build verified for server configuration, SPIFFS mount, wildcard/static handling, and multipart POST `/update` receive path | Physical HTTP verification pending |
| WEB-010--WEB-013 static-file and firmware identity policy | Portable core path, MIME, response-text, chunk, and identity policy | Yes for verbatim path construction, directory index, 256-byte rejection, case-sensitive MIME precedence, missing-file text, 256-byte chunks, and exact identity JSON; clean target build verified | Filesystem and HTTP response adapters pending |
| WEB-006--WEB-009 HTTP parser and response policy | Portable core response descriptors plus ESP-IDF HTTP response adapter | Target build verified for default/content-type responses, chunked/static selection, method rejection, query exclusion, exact 400/404/405/413/500 status mapping, and close behavior | Full POST endpoint composition and physical HTTP verification pending |
| WEB-006 JSON input policy | Portable bounded JSON-prefix parser with scalar member lookup | Yes for valid prefix values, embedded NUL termination, trailing-byte tolerance, ASCII case-insensitive member names, first duplicate selection, and case-sensitive string contents; clean target build verified | Endpoint/cJSON adapter integration pending |
| WEB-020--WEB-022 camera resolution endpoint | Application endpoint with ESP-IDF HTTP adapter over the replaceable camera-resolution port | Yes for 63-byte body limit, empty/invalid/missing/type errors, normalization, sensor failure, success text, status codes, URI registration, and target build verified | Physical HTTP/sensor verification pending |
| WEBUP-001 multipart Content-Type policy | Portable bounded boundary parser | Yes for 1--512-byte header limits, required marker, suffix preservation, matching outer quotes, and ESP-IDF header adapter; clean target build verified | Physical HTTP verification pending |
| WEBUP-002--WEBUP-003 multipart first-part extraction | Portable streaming extractor with replaceable transport feed | Yes for split headers/boundaries, first-marker termination, 4096-byte header limit, terminator-at-limit handling, EOF after headers, and ESP-IDF 1024-byte receive loop; clean target build verified | Physical HTTP verification pending |
| WEB-003, WEB-008 main/video route selection | Portable case-sensitive route table | Yes for all main endpoints, both WebSockets, query exclusion, GET static fallback, and method rejection; clean target build verified | ESP-IDF handler registration target-built; physical HTTP verification pending |
| WEB-011 static-file serving | Application service with replaceable file and response ports | Yes for exact 404 body, path resolution, MIME selection, 256-byte chunks, terminating empty chunk, and close behavior; clean target build verified | SPIFFS and HTTP response adapters pending |
| WEB-002 web-volume startup | Application mount/format policy plus ESP-IDF SPIFFS adapter | Yes for nonfatal mount failure, one format, one retry, and target SPIFFS registration; host suite and target build verified | Physical flash/filesystem verification pending |
| WEB-001 HTTP listener startup | ESP-IDF listener adapter over immutable main/video configuration | Target build verified for ports, control ports, socket limits, backlog, waits, wildcard matching, keepalive/LRU policy, and independent startup failure logging | URI handlers, WebSockets, and physical network verification pending |
| WEB-011, WEB-013 main HTTP handlers | ESP-IDF VFS/HTTP adapter over static-file and identity policies | Target build verified for wildcard static handler, SPIFFS-backed reads, chunked response calls, 404 path, MIME selection, and exact firmware-info payload | Runtime HTTP/SPIFFS verification pending |
| WEBUP-010--WEBUP-014 direct application update | Application OTA orchestration with replaceable camera, partition, OTA, response, and restart ports | Yes for camera-first order, partition/erase/begin/write/finish/boot failures, aborts, empty-image rejection, exact text, two-second delay, restart, multipart HTTP composition, and ESP-IDF OTA adapter; clean target build verified | Physical HTTP/OTA verification pending |
| WEBUP-020--WEBUP-023 direct web-volume update | Application raw partition orchestration with replaceable partition/response/restart port | Yes for partition lookup, full erase before offset-zero write, capacity rejection, empty content, exact errors/success, two-second delay, restart, multipart HTTP composition, and ESP-IDF SPIFFS partition adapter; clean target build verified | Physical HTTP/flash verification pending |
| LIVE-001, LIVE-005--LIVE-006, LIVE-008 live control ownership | Application policy composed with ESP-IDF WebSocket input, preemption response, and generation-bound continuous camera stream | Yes for exact start/stop matching, owner-only stop, cross-socket preemption, same-socket restart suppression, disconnect cleanup, text-frame filtering, exact preemption response, and JPEG frame send; target build verified | Physical camera/WebSocket verification pending |
| MEDIA-003--MEDIA-005 media preemption messages | Portable compact JSON message formatter with ESP-IDF WebSocket preemption sends | Yes for exact namespaces, field order, optional preview session, escaping, no trailing whitespace, and live/preview target routing; clean target build verified | Physical WebSocket verification pending |
| PREV-010 preview path policy | Portable exact `/sd/videos` allow-list | Yes for root/descendant acceptance, root-boundary matching, and any `..` rejection; clean target build verified | Filesystem existence/regular-file adapter pending |
| AVI-001--AVI-005 preview acceptance | Portable RIFF scanner and indexed frame reader | Yes for RIFF/AVI bounds, padded chunk traversal, last metadata/index retention, immediate invalid-index rejection, movi offset reads, 00dc validation, advertised/buffer limits, and short-read rejection; clean target build verified | Storage seek/read adapter and preview session pending |
| PREV-001--PREV-007 preview JSON request policy | Application parser over portable JSON core | Yes for exact `vpreview` namespace, six commands, sequence/default selectors, truncation, and path/session bounds; clean target build verified | WebSocket framing, queue, and session adapters pending |
| PREV-013--PREV-014 preview metadata response | Application formatter over accepted AVI metadata with ESP-IDF preview-open WebSocket adapter | Yes for ordered open/meta fields, filename extraction, timing, dimensions, frame count, JPEG stream marker, path admission, AVI parse, and text response send; target build verified | Persistent session/frame playback and physical storage verification pending |
| PREV-010--PREV-012, PREV-015 preview open admission | Application policy over path and AVI ports | Yes for path/file/AVI failure ordering, random session formatting, largest-index sizing, small-buffer default, and 262144-byte limit; clean target build verified | Filesystem, allocator, response, and stream-start adapters pending |
| PREV-020--PREV-027 preview command policy | Application state machine composed with persistent ESP-IDF preview WebSocket session state | Yes for conflict handling, wrong-session suppression, play selection, pause/resume, clamped seek, stop termination, session identity, and command responses; target build verified | Frame I/O loop, timer task, and physical playback verification pending |
| PREV-020--PREV-022 preview response formatting | Portable application JSON formatter | Yes for command/sequence/error/session ordering, optional message omission, escaping, and exact conflict text; clean target build verified | WebSocket send adapter pending |
| PREV-028--PREV-029 preview frame scheduling | Portable frame-step policy composed with indexed AVI read, FreeRTOS timer task, connection-state checks, and ESP-IDF asynchronous binary WebSocket send | Yes for one-frame delivery, ten-millisecond delay rounding, minimum delay, failure termination, clean EOS gating, indexed JPEG reads, generation-based stop/seek cancellation, connection-close detection, and asynchronous frame transport; target build verified | Physical playback verification pending |
| PREV-001, PREV-006 preview WebSocket input boundary | Application adapter over normalized request parser and ESP-IDF preview-open response | Yes for text-only acceptance, empty-message ignore, binary-message ignore, parser delegation, path/AVI admission, metadata formatting, and WebSocket text send; target build verified | Persistent session/frame playback pending |
| WEB-003, WEB-008 WebSocket route registration | ESP-IDF HTTP server adapter with configurable WebSocket support | Yes for separate `/ws_video` and `/ws_preview` GET handlers, WebSocket Kconfig default, live policy input, and JPEG frame send composition; target build verified | Preview session and continuous live-stream adapters pending |
| PREV-001, PREV-006 WebSocket frame receive adapter | ESP-IDF `httpd_ws_recv_frame` adapter over application input policy | Yes for frame-length probing, bounded payload receive, text/empty filtering, parser delegation, and live-control command dispatch; target build verified | Preview session and response-send adapters pending |
| AVI-010--AVI-013 recorded AVI writer | Portable in-memory MJPEG AVI writer | Yes for the 224-byte little-endian header, 10 fps fields, dimensions, padded JPEG frames, frame counts, movi size, and indexed `idx1` entries; host tests and target build verified | Filesystem segment and recording-service adapters pending |
| REC-003--REC-010 recording segment policy | Application naming and interval policy plus target JPEG capture, finalized-AVI file, and FreeRTOS recording-task ports | Yes for retained-path validation, UTC base naming, 255-byte bound, attempted/successful frame accounting, zero-limit close, 31 inactive-interval close, bounded ESP-IDF JPEG capture, configured recording dimensions, normalized configuration interval/segment limits, controller-status/play condition bridge, short-write handling, flush, and durable file close; host tests and target build verified | Physical SD/camera verification pending |
| REC-001--REC-002 recording command policy | Application command policy over shared text recognition | Yes for M951/M952 state transitions, exact `0xa2` response type and `ok\n` payload, and silent unrelated commands; host tests and target build verified | Router persistence and recording-task composition pending |
| REC-020--REC-024 storage retention policy | Application retention evaluator over storage-directory port | Yes for MiB truncation, 65-percent threshold, case-insensitive AVI/MP4 filtering, regular-file/path bounds, oldest-first ordering, failure skip, and 60-check persistence cadence; host tests and target build verified | ESP-IDF runtime verification pending |
| REC-021--REC-024 storage retention service | Application service over replaceable usage/list/delete/persistence ports | Yes for check cadence, candidate deletion, usage refresh after success, stop-below-threshold behavior, runtime-counter persistence delegation, and target task startup; host tests and target build verified | Physical SD/runtime verification pending |
| REC-021--REC-024 filesystem adapter | ESP-IDF POSIX directory/stat/unlink adapter and FAT usage provider | Yes for `/sd/videos` enumeration, regular-file classification, modification timestamps, bounded path construction, deletion, `esp_vfs_fat_info` usage, and nonfatal task startup; target build verified | Physical SD mount and runtime verification pending |
| HW-030--HW-032, BOOT-013--BOOT-014 SD card adapter | ESP-IDF SDMMC slot-1 lifecycle adapter | Yes for synchronous boot mount, 20 MHz limit, four-bit GPIO mapping, active-low GPIO2 detection, idempotent handoff to debounce lifecycle, nonfatal absence, mount/unmount, and FAT capacity access; target build verified | Physical card and logging verification pending |
| BOOT-013, DIAG logging SD adapter | ESP-IDF buffered POSIX log port composed with `DiagnosticLogWriter` | Yes for append buffering, flush/close, rotation file operations, session/shutdown hooks, target composition, and UTC/uptime timestamps; target build verified | Physical SD write verification pending |
| DIAG capture global hook | ESP-IDF `esp_log_set_vprintf` adapter over bounded `DiagnosticCapture` | Yes for preserving original console output, bounded formatting, nonblocking capture path, UTC fallback timestamps, early startup installation before SD mount, and target startup installation; target build verified | Physical SD logging verification pending |
| DIAG capture-to-log composition | ESP-IDF SD-monitor drain over `DiagnosticLogWriter` | Yes for oldest-first queue consumption and writer handoff while the SD log is active; target build verified | Physical concurrency and write verification pending |
| BOOT-001--BOOT-003 persistence adapter | ESP-IDF NVS key/value adapter | Yes for namespace/key open, string and 64-bit scalar reads/writes, missing-key distinction for strings, commit handling, and target build verification | Runtime, serial, and update compositions target-built; physical NVS verification pending |
| RUN-030--RUN-035 runtime persistence backend | ESP-IDF NVS adapter over `RuntimeCounterPort` | Yes for exact runtime keys, signed first-boot missing/present/failure mapping, unsigned counter reads, writes, and target build verification | Runtime service startup composition pending |
| RUN-030--RUN-035 runtime startup composition | ESP-IDF FreeRTOS runtime-counter task | Yes for NVS-backed initialization, monotonic baseline, and one-second persistence scheduling; target build verified | Wall-clock first-boot recording and play-state notifications pending |
| RUN-031 wall-clock first-boot bridge | Target runtime-task notification function | Yes for forwarding successful wall-clock first-boot values to `RuntimeCounterService`; target build verified | Concrete wall-clock port and play-state bridge pending |
| RUN-020--RUN-023 wall-clock target adapter | ESP-IDF POSIX `time`/`settimeofday` adapter | Yes for Unix reads, zero-microsecond set, UTC formatting, diagnostic forwarding, and first-boot notification; target build verified | Command-routing composition pending |
| RUN-020--RUN-023 wall-clock command composition | Target dispatcher over `WallClockService` | Yes for general-command filtering and exact `time` prefix dispatch; target build verified | UART receive-loop wiring target-built; physical controller verification pending |
| RUN-020--RUN-023 controller command wiring | ESP-IDF UART task over controller stream decoder | Yes for UART initialization, bounded reads, incremental framing, and dispatch of decoded wall-clock commands; target build verified | Full command-family routing and response transport pending |
| RUN-043 controller clearftm wiring | ESP-IDF UART task over `RuntimeCommandService` and NVS runtime adapter | Target build verified for controller-origin `clearftm` admission, exact persistence semantics, and framed UART responses | Physical controller/NVS verification pending |
| RUN-020--RUN-023 wall-clock response transport | UART-bound wall-clock adapter over common frame encoder | Yes for response type/payload framing and single UART write submission; target build verified | Other command-family responses pending |
| RUN-010 serial-number target composition | NVS/UART adapter over `SerialNumberService` | Yes for NVS read/write state mapping, operation admission, command recognition, and framed response transport; target build verified | Physical routing and persistence verification pending |
| REC-001 recording command wiring | Controller-loop dispatch over recording command policy | Yes for M951/M952 recognition, request-state update, exact `0xa2 ok\n` frame encoding, and UART submission; target build verified | Recording task consumption pending |
| TCP-001--TCP-002 TCP control listener | ESP-IDF IPv4 socket accept task | Yes for port 2222, backlog four, four active slots, TCP_NODELAY, keepalive 5/3/1, and 10/5 second timeouts; target build verified | Runtime socket stress verification pending |
| TCP-003--TCP-005, TCP-010 TCP receive composition | ESP-IDF per-client task over independent stream decoders and host identities | Yes for four-slot admission, retrying exact `0x91` overflow response with 1-second per-call timeout, 300 ms rejection delay, 2048-byte reads, partial-frame preservation, routing-decision invocation, destination dispatch, ownership cleanup, and close/decrement behavior; target build verified | Runtime socket stress verification pending |
| TCP-006--TCP-008 transmit queue policy | Portable bounded FIFO | Yes for 32-item capacity, empty/8300-byte frame bounds, FIFO ordering, and retaining the front frame until transport completion; host tests and target build verified | Runtime queue stress verification pending |
| TCP-007--TCP-008 whole-frame sender policy | Portable send callback policy | Yes for short-write continuation, indefinite temporary-failure retry, and permanent-failure termination; host tests and target build verified | Runtime socket stress verification pending |
| TCP-004, TCP-006--TCP-008 TCP client session composition | Portable per-client session | Yes for incremental decoding, stable TCP identity, encoded response queueing, bounded transmit ownership, listener callback, and router/service wiring; host tests and target build verified | Runtime socket stress verification pending |
| ROUTE-001--ROUTE-018 TCP dispatch composition | Portable callback dispatcher over `Router` | Yes for selected controller/local/file/play sinks, concrete target sinks, and ownership-aware file routing; host tests and target build verified | Runtime service integration verification pending |
| TCP controller forwarding | Target FreeRTOS bridge over `ControllerFrameForwarder` | Target build verified for mutex-protected enqueue, controller-task-only UART writes, existing 10 ms spacing, and queue failure isolation | Physical UART and runtime contention verification pending |
| TCP dispatcher target wiring | ESP-IDF TCP task using `TcpFrameDispatcher` | Target build verified for controller, local, file, filesystem-query, and play-status sinks | Runtime service integration verification pending |
| TCP-006--TCP-008 target transmit drain | ESP-IDF client task over `TcpTransmitQueue` and `TcpFrameSender` | Target build verified for FIFO drain, pop-after-completion, short-write continuation, temporary retry, and permanent-failure connection close | Runtime socket stress verification pending |
| ROUTE local-command classification | Portable `LocalCommandFamily` mapping over `CommandKind` | Yes for runtime, serial, recording, filesystem, WLAN, and unknown mappings; host tests and target build verified | Runtime service integration verification pending |
| TCP origin-aware dispatch | `TcpFrameDispatcher` with explicit `TcpClientSession` context | Yes for per-client sink invocation and response queueing without global client lookup; host tests and target build verified | Runtime status and play-status sinks remain separate physical integrations |
| REC-001 TCP-local recording control | ESP-IDF local sink over `RecordingRequestState` and recording command policy | Target build verified for M951/M952 recognition, shared request-state updates, exact response frame queuing, and origin-preserving delivery | Runtime TCP recording verification pending |
| RUN-010 TCP-local serial number | ESP-IDF `TcpSerialNumberAdapter` over NVS and `SerialNumberService` | Target build verified for serial get/set routing, NVS delegation, validation reuse, and per-session response queueing | Runtime NVS/TCP verification pending |
| RUN-030 TCP-local runtime commands | ESP-IDF `TcpRuntimeCommandAdapter` over NVS and `RuntimeCommandService` | Target build verified for `sys-time`, `clearftm`, persisted counters, UTC-minute formatting, erase semantics, and per-session responses | Runtime NVS/TCP verification pending |
| FILE-020--FILE-026 TCP-local filesystem commands | ESP-IDF `TcpFilesystemAdapter` over `FilesystemCommands` | Target build verified for mkdir/remove/rename/ftype dispatch, POSIX operation delegation, cache-path policy reuse, and origin-aware response queueing | Runtime filesystem/TCP verification pending |
| NET-030--NET-033 TCP-local WLAN scan | ESP-IDF `TcpWlanScanAdapter` over `WlanScanCommand` | Target build verified for blocking scan, hidden-network flag, dwell settings, bounded observations, connected SSID selection, and origin-aware responses | Wi-Fi initialization and physical scan verification pending |
| NET-020--NET-026 station adapter | ESP-IDF `TcpWlanStationAdapter` over `StationConnectionPort` | Target build verified for STA mode/configuration, connect/disconnect requests, association/IP snapshots, bounded polling, and nonfatal API mapping | Credential persistence, event integration, and physical Wi-Fi verification pending |
| NET-010 credential persistence | TCP station adapter over shared NVS | Target build verified for `wifi` namespace `ssid`/`password` writes after successful connection; physical NVS/Wi-Fi verification pending | Runtime persistence verification pending |
| NET-040--NET-041 WLAN request parsing | Portable `parse_wlan_request` | Yes for scan fallback, first two non-option tokens, ESC decoding, `-e` ignore, and `-d` precedence; host tests and target build verified | Target command integration pending |
| NET-044--NET-046 TCP WLAN connection flow | ESP-IDF TCP response/STA adapters over `WlanConnectionCommand` | Target build verified for parsed connect/disconnect dispatch, bounded station policy reuse, exact response frames, and origin-aware queueing | Discovery burst and physical Wi-Fi verification pending |
| DISC-001--DISC-008 UDP discovery target adapter | ESP-IDF UDP port over `DiscoveryService` | Target build verified for broadcast socket lifecycle, temporary three-copy burst sockets, destination/payload delegation, and timing policy | Station subnet event wiring and physical network verification pending |
| DISC station address wiring | TCP WLAN connect composition | Target build verified for forwarding successful station IPv4 and actual STA netmask before the command burst | Event timing and physical network verification pending |
| DISC station disconnect wiring | TCP WLAN disconnect composition | Target build verified for clearing station destination after successful disconnect | Physical network verification pending |
| DISC station event wiring | ESP-IDF `WlanEventAdapter` | Target build verified for clearing discovery on STA disconnect and forwarding assigned IPv4/netmask with active-client count | Physical event timing and network verification pending |
| REC-001--REC-002 recording request state | Target atomic state port shared by command and media tasks | Yes for transport-independent request storage, command-loop updates, controller-status/play condition bridge, and task consumption; target build verified | Camera/recording task timing pending |
| REC-002 recording condition policy | Portable application policy | Yes for requiring recording request plus streamed-play or controller-running state; host tests and target build verified | Recording task timing/camera composition pending |
| RUN-033--RUN-035 play-state bridge | Target runtime observer | Yes for forwarding play start/stop transitions with monotonic timestamps to runtime accounting; target build verified | Controller/play composition pending |
| BOOT-001--BOOT-003 persistent-store recovery | ESP-IDF composition root | Build verification only | No |
| HW-002 partition table | Target configuration | Build verification only | No |
| HW-004--HW-005 CPU, flash, PSRAM, security | Target defaults | Build verification only | No |
| HW-060--HW-061 heartbeat | ESP-IDF composition root | Build verification only | No |

All unlisted requirements remain pending. This table deliberately avoids
claiming conformance based only on code presence.
